import React from 'react';
import Contador from './Contador'; // Asegúrate de que la ruta es correcta

const App = () => {
    return (
        <div>
            <Contador />
        </div>
    );
};

export default App;
